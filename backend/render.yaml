services:
  - type: web
    name: finance-calculator-api
    env: java
    buildCommand: ./gradlew bootJar
    startCommand: java -jar build/libs/finance-calculator.jar
    envVars:
      - key: PORT
        value: 8080
      - key: APP_CORS_ALLOWED_ORIGINS
        value: https://your-app.vercel.app
